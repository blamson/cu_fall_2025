#import "@preview/thmbox:0.3.0": *

= Homework 14

== Problem

State the given corollary for all canonical form LPs. Visualize all 4 cases with a small sketch. No explanation or justification is needed.

#corollary[
  A feasible point $#overline[x]$ is optimal for $(P)$ if and only if there exists a vector $y >= 0$ such that $A_(#overline[x])^T y = c$.
]

== Solution

#image("../images/hw14.jpeg")
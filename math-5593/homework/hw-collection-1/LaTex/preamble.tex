% Head related stuff
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhead[R]{MTH 5593 - Linear Programming}  
\fancyhead[L]{Homework Collection 1} 
\fancyfoot[R]{Brady Lamson}
\fancyfoot[L]{CU Denver}

% Math packages
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb} % all for a stupid checkmark
\usepackage{setspace}
\usepackage{graphicx}
\graphicspath{ {./images/}}

\setstretch{1.25}

% Plotting packages
%\usepackage{pgfplots}
%\pgfflotsset{width=10cm, compat=1.9}

% Creates quick matrix of any type ----
% NOTE: p for paran, b for brackets, B for curly braces, v for pipes, V for double pipes 
\newcommand{\m}[2]{\begin{#1matrix}#2\end{#1matrix}}

% Creates small p-matrix ----
\newcommand{\psmall}[1]{
	\left(\begin{smallmatrix}
		#1
	\end{smallmatrix} \right)
}

% Makes problem and solution statements less annoying
\newcommand{\prob}{\noindent\textbf{Problem:}}
\newcommand{\sol}{\noindent\textbf{Solution:}}

% makes mathbb commands faster to type
\newcommand{\bb}{\mathbb}

\newtheorem*{definition}{Definition}
\newtheorem*{theorem}{Theorem}

\expandafter\def\expandafter\normalsize\expandafter{%
    \normalsize%
    \setlength\abovedisplayskip{0pt}%
    \setlength\belowdisplayskip{2pt}%
    \setlength\abovedisplayshortskip{-2pt}%
    \setlength\belowdisplayshortskip{2pt}%
}

% Below is for including code in homework writeups
% To create a code environment use \begin{lstlisting}
\usepackage{listings}
\usepackage{xcolor}

% For AMPL code stuff
\lstdefinelanguage{AMPL}{
  keywords={param, var, minimize, maximize, subject, set, end},
  sensitive=true,
  morecomment=[l]{\#},
  morestring=[b]"
}

\lstset{
  language=AMPL,
  basicstyle=\ttfamily\small,
  keywordstyle=\color{blue}\bfseries,
  commentstyle=\color{gray}\itshape,
  stringstyle=\color{green!50!black},
  numbers=left,
  numberstyle=\tiny\color{gray},
  stepnumber=1,
  frame=single,
  breaklines=true,
  tabsize=2
}

% For python code blocks
% To use, compile with -shell-escape
\usepackage{minted}